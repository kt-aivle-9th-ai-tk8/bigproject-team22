# 로컬 개발용 자격증명 오버라이드. `cp .env.example .env` 후 값 수정.
# docker compose 는 .env 를 자동 로드한다. (미설정 시 아래 기본값 사용)
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=windfarmonm
DB_USERNAME=windfarmonm
DB_PASSWORD=windfarmonm
REDIS_PASSWORD=localredis
